create table quotes (
    asset_type text,
    asset_main_type text,
    cusip text,
    asset_sub_type text,
    symbol text,
    description text,
    bid_price decimal,
    bid_size integer,
    bid_id text,
    ask_price decimal,
    ask_size decimal,
    ask_id text,
    last_price decimal,
    last_size integer,
    last_id text,
    open_price decimal,
    high_price decimal,
    low_price decimal,
    bid_tick text,
    close_price decimal,
    net_change decimal,
    total_volume integer,
    quote_time_in_long bigint,
    trade_time_in_long bigint,
    mark decimal,
    exchange text,
    exchange_name text,
    marginable boolean,
    shortable boolean,
    volatility decimal,
    digits integer,
    fifty_two_wk_high decimal,
    fifty_two_wk_low decimal,
    nav decimal,
    pe_ratio decimal,
    div_amount decimal,
    div_yield decimal,
    div_date text,
    security_status text,
    regular_market_last_price decimal,
    regular_market_last_size decimal,
    regular_market_net_change decimal,
    regular_market_trade_time_in_long decimal,
    net_percent_change_in_double decimal,
    mark_change_in_double decimal,
    mark_percent_change_in_double decimal,
    regular_market_percent_change_in_double decimal,
    delayed boolean
);

create table candles (
    symbol text,
    open decimal,
    high decimal,
    low decimal,
    close decimal,
    volume bigint,
    datetime bigint
)
