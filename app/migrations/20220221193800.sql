create table recommendations (
    ask_price decimal,
    three_month_precent decimal,
    year_percent decimal,
    interpolated_price_percentage decimal,
    interval_progress_weight decimal,
    combined_and_weighted_price_percentage decimal,
    range_high decimal,
    range_low decimal,
    buy boolean
);
